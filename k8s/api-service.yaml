apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: fase1
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: bestsellers
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: api
  ports:
    - name: http
      port: 8000
      targetPort: http
