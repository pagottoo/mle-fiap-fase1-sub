apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: fase1
  labels:
    app.kubernetes.io/name: dashboard
    app.kubernetes.io/part-of: bestsellers
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dashboard
  ports:
    - name: http
      port: 8501
      targetPort: http
